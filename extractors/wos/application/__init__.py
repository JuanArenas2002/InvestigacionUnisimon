"""
Capa de aplicación del extractor Web of Science.

Orquesta las llamadas a la infraestructura (HTTP) usando la lógica
del dominio (parseo, queries). No contiene reglas de negocio puras
ni accede directamente a I/O externo.
"""
